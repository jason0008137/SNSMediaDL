# 預設讓 git 自行判斷文字檔並正規化為 LF
* text=auto

# Windows 專用的啟動腳本必須是 CRLF：
#   - .bat 用 LF 在某些 cmd.exe 情境下會解析錯誤
#   - .ps1 同樣以 Windows 為主要執行環境
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

*.sh text eol=lf

# 二進位，不做 EOL 轉換
*.png binary
*.jpg binary
*.mp4 binary
*.zip binary
*.db binary
